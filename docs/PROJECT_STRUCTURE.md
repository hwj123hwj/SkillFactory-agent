# 📁 SkillFactory Agent - 项目结构

## 目录结构

```
SkillFactory_agent/
├── .claude/                    # Claude Code 配置
│   └── skills/                 # Skills 目录
│       └── skill-browser-crawl/  # 网页爬取 Skill
│
├── docs/                       # 📚 文档目录
│   ├── DOCS.md                 # 文档索引
│   ├── QUICKSTART.md           # 快速开始
│   ├── PROJECT_STRUCTURE.md    # 项目结构（本文档）
│   ├── PRD.md                  # 产品需求
│   └── Agent SDK 参考 - Python.md  # SDK 参考
│
├── src/                        # 源代码
│   ├── __init__.py
│   ├── config.py               # 配置管理
│   ├── models.py               # 数据模型
│   ├── orchestrator.py         # 主调度器（并发管理）
│   ├── worker.py               # Worker Agent（技能孵化）
│   └── utils/
│       ├── __init__.py
│       └── docker.py           # Docker 沙盒执行
│
├── data/                       # 数据目录
│   ├── skills_todo.json        # 任务清单（输入）
│   └── results_log.json        # 执行结果（输出）
│
├── logs/                       # 日志目录
│   └── agent.log               # 运行日志
│
├── README.md                   # 项目主文档
├── .env                        # 环境变量配置（需创建）
├── .env.example                # 环境变量模板
├── pyproject.toml              # 项目配置（uv）
├── run_agent.py                # 主入口
└── test_docker.py              # Docker 功能测试
```

## 核心文件说明

### 配置文件

- **.env** - 环境变量配置（需从 .env.example 复制并填写）
- **pyproject.toml** - Python 项目配置（uv 包管理器）

### 源代码

- **src/config.py** - 配置管理，支持环境变量覆盖
- **src/models.py** - 数据结构定义（SkillSpec, SkillResult）
- **src/orchestrator.py** - 主调度器，管理并发任务
- **src/worker.py** - Worker Agent，负责单个技能孵化
- **src/utils/docker.py** - Docker 沙盒执行器

### 数据文件

- **data/skills_todo.json** - 技能孵化任务清单（输入）
- **data/results_log.json** - 执行结果日志（输出）

### 文档

- **README.md** - 完整的项目文档和使用说明
- **docs/QUICKSTART.md** - 5 分钟快速上手指南
- **docs/PROJECT_STRUCTURE.md** - 项目结构说明（本文档）
- **docs/PRD.md** - 产品需求文档，包含当前实现状态
- **docs/Agent SDK 参考 - Python.md** - Claude Agent SDK 参考文档
- **docs/DOCS.md** - 文档索引和导航

## 生成的文件

技能孵化后会生成以下文件：

```
~/.ai_skills/                   # 技能存储目录
├── skill-name/                 # 单个技能目录
│   ├── SKILL.md                # 技能文档
│   ├── scripts/
│   │   ├── demo.py             # 演示代码
│   │   └── requirements.txt    # 依赖清单
│   └── references/
│       └── research.md         # 研究总结（可选）
└── ...
```

## 工作流程

1. **配置** - 编辑 `data/skills_todo.json` 添加任务
2. **运行** - 执行 `uv run python run_agent.py`
3. **监控** - 查看 `logs/agent.log` 日志
4. **结果** - 查看 `~/.ai_skills/` 生成的技能

## 依赖管理

使用 `uv` 作为包管理器：

```bash
# 安装依赖
uv sync

# 运行程序
uv run python run_agent.py

# 添加新依赖
uv add package-name
```

---

**版本**: v2.0  
**最后更新**: 2026-02-06

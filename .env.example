# SkillFactory Agent - 环境变量配置
# 复制此文件为 .env，并填入实际的配置值

# ============================================
# 并发配置
# ============================================
# MAX_CONCURRENT_WORKERS: 同时孵化的技能数量
# 
# 说明：
# - 设置为 1: 串行执行（一次只孵化一个技能）
# - 设置为 2+: 并行执行（同时孵化多个技能）
#
# 推荐配置：
# - 4C4G 服务器: 1（串行，内存占用低）
# - 8C8G+ 服务器: 2-3（并行，提升效率）
#
# 注意：Claude Code Agent 本身比较占内存（~1-2GB），
#       并发数过高会导致 OOM（内存不足）
MAX_CONCURRENT_WORKERS=1

# 每个 Worker 的超时时间（秒）
# - 串行模式（并发=1）: 建议 900-1200 秒（15-20分钟）
# - 并行模式（并发>1）: 建议 600 秒（10分钟）
WORKER_TIMEOUT=900

# ============================================
# Claude Agent SDK 配置
# ============================================
# ⚠️ 重要：请设置真实的 Claude API 密钥
# 如果使用其他兼容 Anthropic API 的厂商，请设置 ANTHROPIC_AUTH_TOKEN
CLAUDE_API_KEY=your-claude-api-key-here

# 自定义 API 端点（可选，用于使用其他厂商的模型）
# ANTHROPIC_BASE_URL=https://api.anthropic.com
# ANTHROPIC_AUTH_TOKEN=your-custom-api-key-here

# Claude 模型
CLAUDE_MODEL=claude-3-5-sonnet-20241022

# 权限模式（bypassPermissions | acceptEdits | default）
PERMISSION_MODE=bypassPermissions

# ============================================
# Context 7 MCP 配置
# ============================================
# ⚠️ 重要：请设置真实的 Context 7 API 密钥
CONTEXT7_API_KEY=your-context7-api-key-here
CONTEXT7_API_URL=https://mcp.context7.com/mcp

# ============================================
# Docker 配置
# ============================================
# Docker 镜像
DOCKER_IMAGE=python:3.10-slim

# Docker 容器超时时间（秒，默认 5 分钟）
DOCKER_TIMEOUT=300

# ============================================
# Worker Agent 配置
# ============================================
# 代码测试失败后的最大重试次数
MAX_RETRY_ATTEMPTS=3

# ============================================
# 项目路径配置（通常无需修改）
# ============================================
# SKILLS_DIR=~/.ai_skills  # 硬编码在 config.py 中
# DATA_DIR=./data          # 硬编码在 config.py 中
# LOGS_DIR=./logs          # 硬编码在 config.py 中

# ============================================
# 日志配置
# ============================================
LOG_LEVEL=INFO
